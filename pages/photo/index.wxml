<!--index.wxml-->
<view class="container">
  <image class='banner' src='./banner.jpg'></image>
    <view class="item" wx:key="{{index}}" wx:for="{{photos}}">
      <text>{{item.date}}</text>
      <view>
        <image src="{{item.src}}"></image>
      </view>
    </view>
</view>
