<!--index.wxml-->
<view class="container">
  <view scroll-x="true" style="width: 1000rpx">
    <view class="item" wx:key="{{index}}" wx:for="{{photos}}">
      <image src="{{item.src}}"></image>
      <text>{{item.date}}</text>
    </view>
  </view>
</view>
